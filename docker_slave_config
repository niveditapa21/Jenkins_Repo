 Install Docker:
 **************
 sudo apt update
 sudo apt install docker.io -y

 Add user to docker group:
 *************************
 usermod -aG docker {user-name}  --->  ex: usermod -aG docker jenkins
 systemctl restart docker


Now, restart Jenkins:
*********************
http://<ec2-instance-public-ip>:8080/restart
